//全局参数
ext {
    appConfig = [
            versionCode  : 1,
            versionName  : "1.0",
            applicationId: "com.ckr.collapsingrefresh",
    ]

    androidConfig = [
            compileSdkVersion: 26,
            minSdkVersion    : 16,
            targetSdkVersion : 26,
    ]

    supportVersion = "26.1.0"
}